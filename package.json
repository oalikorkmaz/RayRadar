{
  "name": "rayradar",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "prepare:web": "node scripts/copy-web.js",
    "sync": "npm run prepare:web && npx cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/local-notifications": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.0",
    "@capacitor/cli": "^6.0.0"
  }
}
